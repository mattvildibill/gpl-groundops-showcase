{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "OpsTaskCreated.v1.json",
  "title": "OpsTaskCreated.v1",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "eventId",
    "eventType",
    "timestamp",
    "correlationId",
    "taskId",
    "planId",
    "asset",
    "windowStart",
    "windowEnd",
    "priority",
    "createdBy"
  ],
  "properties": {
    "eventId": { "type": "string", "format": "uuid" },
    "eventType": { "const": "OpsTaskCreated.v1" },
    "timestamp": { "type": "string", "format": "date-time" },
    "correlationId": { "type": "string" },
    "taskId": { "type": "string" },
    "planId": { "type": "string" },
    "asset": { "type": "string" },
    "windowStart": { "type": "string", "format": "date-time" },
    "windowEnd": { "type": "string", "format": "date-time" },
    "priority": { "type": "string", "enum": ["ROUTINE", "PRIORITY", "CRITICAL"] },
    "createdBy": { "type": "string" }
  }
}
